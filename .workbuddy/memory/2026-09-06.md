
## 技能仓库标准化（mdvis/agents）
- 确认 6 个技能 SKILL.md frontmatter 已符合 Agent Skills 标准（name+description，name 与目录一致）
- 新增 `scripts/validate-skills.sh`：校验技能结构合规
- 新增 `.github/workflows/validate.yml`：push 时自动校验
- README 增加 `npx skills add mdvis/agents` 标准安装方式（-g 全局 / --skill 单装 / --all）
- 已推送 8976afc，并用 `npx skills add /Users/mdvis/Agents --list` 实测 CLI 可识别全部技能
